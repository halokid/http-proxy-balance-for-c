# http-proxy-balance-for-c
a http proxy for balance 

a simple http balance program, base on hash for cache function , K/V record cache , cache timeout param


gcc http_balance.c hash.h hash.c -o http_balance
